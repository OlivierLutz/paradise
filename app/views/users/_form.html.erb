<%= bootstrap_form_for(@user) do |f| %>

    <%= f.text_field :email %>    
    <%= f.password_field :password, label: "Mot de passe" %>
    <%= f.password_field :password_confirmation, label: "Confirmation du mot de passe" %>
    
    </br>
    <%= f.submit "Créer votre utilisateur", class: 'btn btn-primary' %>
<% end %>